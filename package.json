{
  "name": "eot_tool",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npx turbo run build",
    "build:wasm": "make wasm",
    "pack-check": "npx turbo run pack-check",
    "test": "npx turbo run test",
    "verify:wasm": "make verify-wasm-artifacts"
  }
}
