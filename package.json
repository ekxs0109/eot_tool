{
  "name": "eot_tool",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.7.0"
  },
  "scripts": {
    "build": "turbo run build",
    "build:benchmark-web": "turbo run build --filter=benchmark-web",
    "build:wasm": "pnpm --filter fonttool-wasm build",
    "dev:benchmark-web": "pnpm --filter benchmark-web dev",
    "pack-check": "turbo run pack-check",
    "test": "turbo run test",
    "verify:wasm": "bash tests/verify_wasm_artifacts.sh"
  },
  "devDependencies": {
    "turbo": "2.9.6"
  }
}
